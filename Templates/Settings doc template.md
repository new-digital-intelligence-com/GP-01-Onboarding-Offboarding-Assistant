# GP-01 Onboarding and Offboarding — Settings — {{tenant}}

*Created by GP-01 with the user's consent. Contains file references and addresses only —
never credentials, tokens or personal data.*

| Setting | Value |
|---|---|
| Provisioning register | {{register_name}} — `{{register_file_id}}` |
| Policy / handbook folder | {{handbook_folder_name}} — `{{handbook_folder_id}}` |
| Welcome email template | {{welcome_template_name}} — `{{welcome_template_id}}` |
| Completion report template | {{report_template_name}} — `{{report_template_id}}` |
| Roster targets | {{roster_targets}} — the lists kept current on every joiner, mover and leaver (team roster sheet, distribution lists, org chart, internal team page), each with its file ID or URL |
| People Ops address | {{people_ops_email}} |
| IT Operations address | {{it_email}} |
| Default approver for approval-gated actions | {{default_approver}} |
| Retention window | {{retention_days}} days |

## Systems seen live at last run

*A hint only. GP-01 re-checks availability at the start of every run — a connector can be
switched off for a chat without anything in this document changing.*

| System | Tier at {{last_run_date}} | Bridge, if any |
|---|---|---|
{{tier_rows}}

## Change log

| Date | Changed by | What changed |
|---|---|---|
{{change_log}}

---

**Known limitation:** the Drive tools cannot reliably edit this document in place. When
settings change, GP-01 writes a new document with the same title and tells you the older
one still exists, so you can delete it. GP-01 always reads the most recently modified
match.
