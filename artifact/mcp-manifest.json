{
  "_comment": [
    "The mcp capability manifest for the published artifact, and the page's own allowlist.",
    "One source for two things that must never disagree: the `capabilities.mcp` value passed",
    "to the Artifact tool at publish time, and the tools the page will forward a call to.",
    "",
    "`server` is the DISPLAY NAME of the connector in the publishing account's claude.ai",
    "Settings -> Connectors. It is not a URL and carries no token: calls run with the",
    "VIEWER's credentials.",
    "",
    "`tools` MUST be the exact upstream tool names that connector exposes. There is no",
    "wildcard, and a wrong name fails at runtime with not_in_manifest.",
    "",
    "`enabled: false` drops a server from the published manifest entirely. The page then",
    "shows that system as not configured and the skill stages its steps with a named owner,",
    "rather than the run failing halfway. Turn one on only once its `tools` list is real.",
    "",
    "`verified` records whether the tool names were confirmed against a live listTools()",
    "call, or only transcribed from references/connector-map.md."
  ],

  "_dbRules_note": [
    "EMPTY ON PURPOSE — asked for on 2026-09-13. Anyone the artifact is shared with can do",
    "everything: run provisioning and read the full run history.",
    "",
    "What that means: run history carries employee names and what was done to their accounts,",
    "so an offboarding row tells every viewer that a named person was terminated — possibly",
    "before it is announced. The default for a db artifact is that every viewer reads and",
    "writes all shared documents, and that is what an empty list restores.",
    "",
    "To lock it down later, put these back and republish. Levels are view < interact (can",
    "view) < admin (can edit) < owner; the owner always passes:",
    "  { \"path\": \"\",        \"read\": \"interact\", \"write\": \"interact\" },",
    "  { \"path\": \"history\", \"read\": \"admin\",    \"write\": \"admin\"    }",
    "",
    "A read below the minimum returns EMPTY rather than an error, so a blocked viewer sees an",
    "empty history and not a permission message. The page's empty state changes to match."
  ],
  "dbRules": [],

  "servers": [
    {
      "server": "Zapier",
      "enabled": true,
      "verified": true,
      "purpose": "Every system. Slack stays here rather than on the native Claude Slack connector, which has no tool to add or remove a channel member — see references/connector-map.md.",
      "systems": ["Google Workspace", "Gmail", "Slack", "Google Sheets"],
      "_tools_note": [
        "Zapier MCP is GENERIC: it does NOT expose one tool per app action. There is no",
        "google_workspace_admin_..._raw_request tool to name here. The flow is:",
        "discover_zapier_actions -> execute_zapier_read_action / execute_zapier_write_action.",
        "",
        "All six names were read off the live connector on 2026-09-12. Note the infix: the",
        "execute and discover tools are *_zapier_*, NOT the bare discover_actions /",
        "execute_read_action / execute_write_action the UI display names suggest."
      ],
      "tools": [
        "discover_zapier_actions",
        "execute_zapier_read_action",
        "execute_zapier_write_action",
        "inspect_zapier_actions",
        "list_zapier_connections",
        "manage_zapier_connections"
      ]
    }
  ]
}
